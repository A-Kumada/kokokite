<!DOCTYPE html>
<html>
  <head>
    <title>Kokokite</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg p-5">
      <div class="container">
      <a class="navbar-brand" href="/">
      <span class="pl-5 pr-5 pt-2 pb-2 border text-dark">ココキテ</span>
      </a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav ml-auto">

        <%= form_tag posts_path, :method => 'get' do %> <%#　projects_pathはモデル名で読みかえること %>
        <p>
        <%= text_field_tag :search, params[:search] %>
        <%= submit_tag "検索", :title => nil %>
        </p>
        <% end %>

        <% if user_signed_in? %>
          <li>
            <%= link_to mypage_path,class:"nav-link text-dark ml-5 " do %>
              <i class="fas fa-user"></i>
              マイページ
            <% end %>
          </li>
          <li>
            <%= link_to new_post_path,class:"nav-link text-dark ml-3 " do %>
            <i class="fas fa-pen"></i>投稿
            <% end %>
          </li>
          <li>
            <%= link_to destroy_user_session_path, method: :delete, class:"nav-link text-dark ml-3" do %>
            <i class="fas fa-sign-out-alt"></i>
            ログアウト
            <% end %>
          <li>

        <% elsif admin_signed_in? %>
          <li>
          <%#= link_to admin_posts_path,class:"nav-link border text-dark" do %>投稿一覧
          <%# end %>
          </li>
          <li>
            <%= link_to admin_users_path,class:"nav-link border text-dark ml-5" do %>会員一覧
            <% end %>
          </li>
          <li>
            <%= link_to destroy_admin_session_path, method: :delete, class:"nav-link border text-dark ml-5" do %>
            ログアウト
            <% end %>
          </li>

        <% else %>
          <li>
            <%= link_to new_user_registration_path, class:"nav-link text-dark ml-5" do %>
              <i class="fas fa-user-plus"></i>
              新規登録
            <% end %>
          </li>
          <li>
            <%= link_to new_user_session_path, class:"nav-link text-dark ml-3" do %>
              <i class="fas fa-sign-in-alt"></i>
              ログイン
            <% end %>
          </li>
          <li>
            <%= link_to users_guest_sign_in_path, method: :post, class:"nav-link text_dark" do %>
              ゲストログイン（閲覧用）
            <% end %>
          </li>
       <% end %>

      </ul>
      </div>
      </div>
      </nav>
    </header>
    <%= yield %>
  </body>
</html>
