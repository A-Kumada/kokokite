<div class="container">
  <div class="row">
    <div class="col-md-12 offset-md-3">
      <table>
        <tr>
          <td>タイトル</td><td><%= @post.title %></td>
        </tr>
        <tr>
          <td>概要</td><td><%= @post.outline  %></td>
        </tr>
        <tr>
          <td>必要なもの</td><td><%= @post.necessaries %></td>
        </tr>
        <tr>
          <td>ポイント</td><td><%= @post.point %></td>
        </tr>
        <tr>
          <td>手順</td>
          <td>
            <% @post.procedures.each do |procedure| %>
            <%= procedure.content %>
            <% end %>
          </td>
        </tr>
      </table>

       <div class="pt-3"><%= link_to edit_post_path,class:"btn btn-success" do %>編集する
       <% end %>

    </div>
  </div>
</div>