<% if flash[:notice] %>
 <div class="alert alert-success" role="alert">
 <p class="notice text-center" style="color:green;"><%= flash[:notice] %></p></div>
<% end %>

<div class="container">
  <div class="row">
    <div class="col-md-12">
      <div class="text-center pt-5 pb-0">
        一人暮らしを始めたあなたへ...<br>
        ここで一人暮らしの生活の知恵や裏技を見つけてみませんか？<br><br>
        一人暮らしのベテランさんは、ぜひおすすめの生活の知恵を教えてください！
      </div>
      <span class="main-visual">
        <%= image_tag 'main.jpg' %>
      </span>
    </div>
  </div>

  <div class="row">
    <div class=" col-md-12 pl-5">
      <h4 class="pt-5 pb-4">カテゴリ一覧</h4>
      <div class="d-flex flex-wrap">
      <% @categories.each do |category| %>
        <div class="rounded pr-3 pb-5">
          <%= link_to category.category_name, posts_path(category_id: category.id), class: "btn-lg btn-info p-3" %>
        </div>
      <% end %>
      </div>

      <h4 class="pt-5 pb-4">新着　NEW！</h4>
      <div class="d-flex flex-wrap pr-3">
        <% @posts.each do |post| %>
        <div class="rounded ml-4 mr-4 pb-4">
          <ul class="navbar-nav">
            <li>
              <%= link_to post_path(post),class: "text-secondary font-weight-bold" do %>
                <% if post.image.attached? %>
                  <%= image_tag post.image, class: "top-thumbnail" %>
                <% else %>
                  <%= image_tag 'no_image.jpg', class: "top-thumbnail" %>
                <% end %>
              <% end %><br>

              <%= link_to post_path(post),class: "text-secondary font-weight-bold" do %>
                <%= post.title.truncate(11) %>
              <% end %><br>
              <span class="rank-nickname-font pl-1">by<%= post.user.nickname %></span>
            </li>
          </ul>
        </div>
        <% end %>
      </div>

      <h4 class="pt-5 pb-4">
        <i class="fas fa-crown"></i>
        アクセスランキング
        <i class="fas fa-crown"></i>
      </h4>
      <div class="d-flex flex-wrap pr-3 pb-5">
        <% @post_pvs.each_with_index do |post, i| %>
        <% if i >= 5 %>
          <% break %>
        <% end %>
        <div class="rounded ml-4 mr-4 pb-4">
          <ul class="navbar-nav ml-auto">
            <li>
              <%= link_to post_path(post),class: "text-secondary font-weight-bold" do %>
                <% if post.image.attached? %>
                  <%= image_tag post.image, class: "top-thumbnail" %>
                <% else %>
                  <%= image_tag 'no_image.jpg', class: "top-thumbnail" %>
                <% end %>
              <% end %><br>

          <%= link_to post_path(post),class: "text-secondary font-weight-bold" do %>
            <%= post.title.truncate(11) %>
          <% end %><br>
          <span class="rank-nickname-font">by <%= post.user.nickname %></span>
          </li>
        </div>
        <% end %>
      </div>
    </div>
  </div>
</div>